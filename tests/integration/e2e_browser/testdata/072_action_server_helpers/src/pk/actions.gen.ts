/* Code generated by piko; DO NOT EDIT. */

import { ActionBuilder, createActionBuilder, registerActionFunction } from '/_piko/dist/ppframework.core.es.js';
export interface SendInput {
  title: string;
}
export interface SendResponse {
  success: boolean;
}
/**
 * SendAction sends a notification and triggers client-side helpers.
 */
export function notifySend(input: SendInput): ActionBuilder<SendResponse> {
  return createActionBuilder<SendResponse>('notify.Send', input);
}
export const action = {
  notify: {
    Send: notifySend
  }
} as const;
registerActionFunction('notify.Send', notifySend);
