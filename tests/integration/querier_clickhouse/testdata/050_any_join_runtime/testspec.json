{
  "description": "ANY LEFT JOIN picks at most one match per left-hand row (instead of expanding to all matches as a regular LEFT JOIN would). Verifies that the parser accepts the ANY modifier on a LEFT JOIN and that the runtime returns the deduplicated row shape."
}
