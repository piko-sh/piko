{
  "description": "Dynamic runtime builder over the database/sql ClickHouse path: exercises .Where/.OrderBy/.Limit placeholder generation against live ClickHouse to verify the runtime builder's bind-placeholder style is compatible with the clickhouse-go driver."
}
