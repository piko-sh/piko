{
  "description": "Combinator -If suffix on count/sum/uniq aggregates. Exercises countIf(predicate), sumIf(column, predicate), and uniqIf(column, predicate) over a small events table. Verifies that the ClickHouse resolver strips the If combinator, resolves the base aggregate, and reports the filtered counts back to a generated Go struct."
}
