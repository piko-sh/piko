{
  "description": "Enum8 column round-trip: inserts rows with all enum variants, fetches them back, and verifies that the generated Go type preserves both the label and the underlying integer tag."
}
