{
  "description": "time_weight('LOCF', ts, value) builds a time-weighted summary state, and average(summary) extracts the time-weighted mean. Verifies the catalogue's time_weight constructor and the matching accessor surface."
}
