{
  "description": "heartbeat_agg(ts, start, agg_interval, heartbeat_interval) records the periods during which a sender was alive. The fixture seeds heartbeats at known offsets and verifies the uptime / downtime / num_live_ranges accessors against the catalogue's heartbeat summary type."
}
