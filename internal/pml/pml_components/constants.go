// Copyright 2026 PolitePixels Limited
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     http://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

// This project stands against fascism, authoritarianism, and all forms of
// oppression. We built this to empower people, not to enable those who would
// strip others of their rights and dignity.

package pml_components

const (
	// CSSWidth is the CSS property name for setting the width of an element.
	CSSWidth = "width"

	// CSSHeight is the CSS property name for setting an element's height.
	CSSHeight = "height"

	// CSSPadding is the CSS padding property name.
	CSSPadding = "padding"

	// CSSMarginLeft is the CSS property name for left margin spacing.
	CSSMarginLeft = "margin-left"

	// CSSBorder is the CSS property name for border styling.
	CSSBorder = "border"

	// CSSBorderStyle is the CSS property name for the border style.
	CSSBorderStyle = "border-style"

	// CSSBorderWidth is the CSS property name for setting the border width.
	CSSBorderWidth = "border-width"

	// CSSBorderColor is the CSS property name for the border colour.
	CSSBorderColor = "border-color"

	// CSSVerticalAlign is the CSS property name for vertical alignment.
	CSSVerticalAlign = "vertical-align"

	// CSSDirection is the CSS direction property for text flow (ltr or rtl).
	CSSDirection = "direction"

	// CSSAlign is the CSS property name for horizontal alignment.
	CSSAlign = "align"

	// CSSTextAlign is the CSS property name for text alignment.
	CSSTextAlign = "text-align"

	// CSSDisplay is the CSS display property name.
	CSSDisplay = "display"

	// CSSFontFamily is the CSS font-family property name.
	CSSFontFamily = "font-family"

	// CSSFontSize is the CSS font-size property name used to set the size of text.
	CSSFontSize = "font-size"

	// CSSFontWeight is the CSS property name for font weight.
	CSSFontWeight = "font-weight"

	// CSSFontStyle is the CSS property name for font style (e.g. italic, normal).
	CSSFontStyle = "font-style"

	// CSSLineHeight is the CSS line-height property name.
	CSSLineHeight = "line-height"

	// CSSColor is the CSS property name for setting text colour.
	CSSColor = "color"

	// CSSTextDecoration is the CSS text-decoration property name.
	CSSTextDecoration = "text-decoration"

	// CSSTextTransform is the CSS property name for setting text case.
	CSSTextTransform = "text-transform"

	// CSSBackground is the CSS background shorthand property name.
	CSSBackground = "background"

	// CSSBackgroundColor is the CSS property name for setting background colour.
	CSSBackgroundColor = "background-color"

	// CSSBackgroundSize is the CSS background-size property attribute name.
	CSSBackgroundSize = "background-size"

	// CSSBackgroundPosition is the CSS property for background positioning.
	CSSBackgroundPosition = "background-position"

	// CSSBackgroundRepeat is the CSS property name for background repeat behaviour.
	CSSBackgroundRepeat = "background-repeat"

	// CSSCursor is the CSS property name for the cursor style.
	CSSCursor = "cursor"

	// CSSMargin is the CSS property name for setting element margins.
	CSSMargin = "margin"

	// CSSBorderCollapse is the CSS property name for border-collapse.
	CSSBorderCollapse = "border-collapse"

	// CSSBorderSpacing is the CSS property name for table border spacing.
	CSSBorderSpacing = "border-spacing"

	// CSSBorderLeft is the CSS property name for the left border.
	CSSBorderLeft = "border-left"

	// CSSBorderRight is the CSS property name for the right border.
	CSSBorderRight = "border-right"

	// CSSBorderTop is the CSS property name for the top border.
	CSSBorderTop = "border-top"

	// CSSBorderBottom is the CSS property name for the bottom border.
	CSSBorderBottom = "border-bottom"

	// CSSMaxWidth is the CSS property name for the maximum width of an element.
	CSSMaxWidth = "max-width"

	// CSSMinWidth is the CSS property name for the minimum width.
	CSSMinWidth = "min-width"

	// CSSMaxHeight is the CSS property name for maximum height.
	CSSMaxHeight = "max-height"

	// CSSOutline is the CSS property name for the outline style.
	CSSOutline = "outline"

	// CSSMsoPaddingAlt is the CSS property name for Outlook-specific padding.
	CSSMsoPaddingAlt = "mso-padding-alt"

	// CSSMsoLineHeightRule is the CSS property name for Microsoft Office line
	// height rule.
	CSSMsoLineHeightRule = "mso-line-height-rule"

	// CSSOverflow is the CSS property name for controlling content overflow.
	CSSOverflow = "overflow"

	// CSSWordBreak is the CSS property name that controls how words break.
	CSSWordBreak = "word-break"

	// CSSWhiteSpace is the CSS property name for controlling whitespace handling.
	CSSWhiteSpace = "white-space"

	// AttrAlign is the HTML align attribute name used to set element alignment.
	AttrAlign = "align"

	// AttrAlt is the HTML alt attribute name for image alternative text.
	AttrAlt = "alt"

	// AttrBackground is the HTML attribute name for a background image.
	AttrBackground = "background"

	// AttrClass is the HTML class attribute name.
	AttrClass = "class"

	// AttrCSSClass is the attribute name for custom CSS classes on elements.
	AttrCSSClass = "css-class"

	// AttrBackgroundURL is the attribute name for the hero background image URL.
	AttrBackgroundURL = "background-url"

	// AttrBackgroundWidth is the attribute name for the background image width.
	AttrBackgroundWidth = "background-width"

	// AttrBackgroundHeight is the attribute name for background image height.
	AttrBackgroundHeight = "background-height"

	// AttrBorder is the attribute name for border styling.
	AttrBorder = "border"

	// AttrBorderLeft is the attribute name for the left border style.
	AttrBorderLeft = "border-left"

	// AttrBorderRight is the attribute name for the right border style.
	AttrBorderRight = "border-right"

	// AttrBorderTop is the attribute name for the top border style.
	AttrBorderTop = "border-top"

	// AttrBorderBottom is the attribute key for the bottom border style.
	AttrBorderBottom = "border-bottom"

	// AttrBorderRadius is the attribute name for the border radius style property.
	AttrBorderRadius = "border-radius"

	// AttrColour is the attribute name for text colour.
	AttrColour = "color"

	// AttrDensities is the attribute name for pixel density options in responsive
	// images.
	AttrDensities = "densities"

	// AttrBackgroundPositionX is the attribute name for the horizontal background
	// position.
	AttrBackgroundPositionX = "background-position-x"

	// AttrBackgroundPositionY is the attribute key for vertical background position.
	AttrBackgroundPositionY = "background-position-y"

	// AttrFluidOnMobile is the attribute name for fluid image scaling on mobile.
	AttrFluidOnMobile = "fluid-on-mobile"

	// AttrMode is the attribute key for setting how the hero section displays.
	AttrMode = "mode"

	// AttrMaxHeight is the attribute name for the maximum height of an image.
	AttrMaxHeight = "max-height"

	// AttrName is the HTML name attribute for anchor elements.
	AttrName = "name"

	// AttrProfile is the attribute name for the image transformation profile.
	AttrProfile = "profile"

	// AttrSizes is the HTML sizes attribute for responsive image selection.
	AttrSizes = "sizes"

	// AttrSrc is the HTML attribute name for the image source URL.
	AttrSrc = "src"

	// AttrSrcset is the HTML srcset attribute for responsive image sources.
	AttrSrcset = "srcset"

	// AttrStackChildren is the attribute name for stacking row children vertically.
	AttrStackChildren = "stack-children"

	// AttrTextPadding is the attribute name for the padding around text in a row.
	AttrTextPadding = "text-padding"

	// AttrUsemap is the HTML usemap attribute for associating an image map.
	AttrUsemap = "usemap"

	// AttrContainerBackgroundColor is the attribute name for the background colour
	// of the outer container element.
	AttrContainerBackgroundColor = "container-background-color"

	// AttrDirection is the attribute name for text direction (ltr or rtl).
	AttrDirection = "direction"

	// AttrFullWidth is the attribute name for full-width display mode.
	AttrFullWidth = "full-width"

	// AttrHeight is the attribute name for setting an element's height.
	AttrHeight = "height"

	// AttrInnerPadding is the attribute name for inner padding on components.
	AttrInnerPadding = "inner-padding"

	// AttrInnerBackgroundColor is the attribute name for the inner background colour.
	AttrInnerBackgroundColor = "inner-background-color"

	// AttrInnerBorder is the attribute name for inner border styling.
	AttrInnerBorder = "inner-border"

	// AttrInnerBorderRadius is the attribute name for inner border radius styling.
	AttrInnerBorderRadius = "inner-border-radius"

	// AttrListStyle is the attribute name for the list style type.
	AttrListStyle = "list-style"

	// AttrPadding is the attribute name for padding values in style definitions.
	AttrPadding = "padding"

	// AttrPaddingTop is the attribute name for top padding on PML elements.
	AttrPaddingTop = "padding-top"

	// AttrPaddingBottom is the attribute name for bottom padding spacing.
	AttrPaddingBottom = "padding-bottom"

	// AttrPaddingLeft is the attribute name for the left padding.
	AttrPaddingLeft = "padding-left"

	// AttrPaddingRight is the attribute name for right padding.
	AttrPaddingRight = "padding-right"

	// AttrMarginBottom is the attribute name for bottom margin spacing.
	AttrMarginBottom = "margin-bottom"

	// AttrType is the attribute name for list type, such as bullet or number.
	AttrType = "type"

	// AttrBulletColor is the style attribute for list item bullet colour.
	AttrBulletColor = "bullet-color"

	// AttrBulletFontSize is the style attribute for list bullet font size.
	AttrBulletFontSize = "bullet-font-size"

	// AttrBulletFontFamily is the style attribute for the bullet font family.
	AttrBulletFontFamily = "bullet-font-family"

	// AttrBulletFontWeight is the style attribute for the bullet font weight.
	AttrBulletFontWeight = "bullet-font-weight"

	// AttrBulletFontStyle is the attribute name for the bullet font style.
	AttrBulletFontStyle = "bullet-font-style"

	// AttrBulletLineHeight is the style attribute for bullet line height.
	AttrBulletLineHeight = "bullet-line-height"

	// AttrStyle is the HTML style attribute name for inline CSS.
	AttrStyle = "style"

	// AttrTarget is the HTML target attribute name for link opening.
	AttrTarget = "target"

	// AttrWidth is the CSS width property name.
	AttrWidth = "width"

	// AttrRole is the HTML role attribute name for accessibility purposes.
	AttrRole = "role"

	// AttrCellPadding is the HTML cellpadding attribute for table elements.
	AttrCellPadding = "cellpadding"

	// AttrCellSpacing is the HTML attribute name for table cell spacing.
	AttrCellSpacing = "cellspacing"

	// AttrDir is the HTML direction attribute for text
	// directionality (ltr/rtl).
	AttrDir = "dir"

	// AttrBgColor is the HTML attribute for setting the background colour.
	AttrBgColor = "bgcolor"

	// AttrHref is the attribute name for link URLs on buttons and images.
	AttrHref = "href"

	// AttrRel is the HTML rel attribute name for link relationships.
	AttrRel = "rel"

	// AttrTitle is the HTML title attribute name for tooltips.
	AttrTitle = "title"

	// AttrValign is the HTML attribute name for vertical alignment.
	AttrValign = "valign"

	// TargetBackground is the style target for background-related properties.
	TargetBackground = "background"

	// TargetBullet is the style target for list item bullet properties.
	TargetBullet = "bullet"

	// TargetCell is the style target for the cell element.
	TargetCell = "cell"

	// TargetContainer is the style target for the outer container element.
	TargetContainer = "container"

	// TargetImage is the style target for the image element itself.
	TargetImage = "image"

	// TargetLink is the style target for the anchor element within a button.
	TargetLink = "link"

	// TargetText is the style target for text content within an element.
	TargetText = "text"

	// ValueAuto is the CSS auto value for automatic sizing or spacing.
	ValueAuto = "auto"

	// ValueCentre is the HTML align attribute value for centred content.
	ValueCentre = "center"

	// ValueLeft is the CSS value for left alignment.
	ValueLeft = "left"

	// ValueRight is the value for right-aligned content.
	ValueRight = "right"

	// ValueLTR is the left-to-right text direction value for layout elements.
	ValueLTR = "ltr"

	// ValueRTL is the value for right-to-left text direction.
	ValueRTL = "rtl"

	// ValueNone is the CSS value for disabling a property such as border or outline.
	ValueNone = "none"

	// ValueRepeat is the CSS value for repeating background images.
	ValueRepeat = "repeat"

	// ValueTop is the CSS value for top vertical alignment.
	ValueTop = "top"

	// ValueMiddle is the vertical alignment value for centering content.
	ValueMiddle = "middle"

	// ValueBottom is the value for bottom vertical alignment.
	ValueBottom = "bottom"

	// ValueZero is the string "0" used for HTML attributes that require a zero value.
	ValueZero = "0"

	// ValueZeroPx is the zero pixel value used for the zero font size technique.
	ValueZeroPx = "0px"

	// Value100 is the percentage value for full width or height.
	Value100 = "100%"

	// ValueInlineBlock is the CSS display value for inline-block layout.
	ValueInlineBlock = "inline-block"

	// ValuePresentation is the ARIA role value for layout tables.
	ValuePresentation = "presentation"

	// ValueBlank is the HTML target attribute value that opens links in a new tab.
	ValueBlank = "_blank"

	// ValueSeparate is the CSS border-collapse value for separated table borders.
	ValueSeparate = "separate"

	// ValueBreakWord is the CSS word-break property value that allows long words to
	// be broken at any character to prevent overflow.
	ValueBreakWord = "break-word"

	// ValueOrdered is the list style value for numbered lists.
	ValueOrdered = "ordered"

	// ValueUnordered is the list style value for a bulleted list.
	ValueUnordered = "unordered"

	// ValueTrue is the string used to represent true in boolean attributes.
	ValueTrue = "true"

	// ValueFalse is the string value for a false boolean attribute.
	ValueFalse = "false"

	// ValueBlock is the CSS display value for block-level elements.
	ValueBlock = "block"

	// ValueHTTP is the URL scheme prefix for plain HTTP connections.
	ValueHTTP = "http://"

	// ValueHTTPS is the HTTPS URL scheme prefix.
	ValueHTTPS = "https://"

	// ValueNoRepeat is the CSS background-repeat value that prevents repetition.
	ValueNoRepeat = "no-repeat"

	// ValueFullWidth is the attribute value that enables full-width row rendering.
	ValueFullWidth = "full-width"

	// ValueMarginAuto is the CSS margin value for horizontal auto-centering.
	ValueMarginAuto = "0px auto"

	// ValueExactly is the CSS mso-line-height-rule value for exact line height.
	ValueExactly = "exactly"

	// ValueHidden is the CSS value for hidden overflow.
	ValueHidden = "hidden"

	// ValueFluidHeight is the mode value for fluid-height hero sections.
	ValueFluidHeight = "fluid-height"

	// ValueFixedHeight is the mode where the hero has a fixed height.
	ValueFixedHeight = "fixed-height"

	// ValueSpace is a single space character used to join CSS property values.
	ValueSpace = " "

	// ValueHalf is the decimal representation of 50% for VML positioning.
	ValueHalf = "0.5"

	// ValueCidPrefix is the URI prefix for Content-ID references in emails.
	ValueCidPrefix = "cid:"

	// ValueEmailDefaultProfile is the default image profile used for email assets.
	ValueEmailDefaultProfile = "email-default"

	// ElementA is the HTML anchor element tag name.
	ElementA = "a"

	// ElementDiv is the HTML div element tag name.
	ElementDiv = "div"

	// ElementImg is the HTML img element tag name.
	ElementImg = "img"

	// ElementLi is the HTML list item element tag name.
	ElementLi = "li"

	// ElementOl is the HTML tag name for an ordered list element.
	ElementOl = "ol"

	// ElementP is the tag name for the HTML paragraph element.
	ElementP = "p"

	// ElementSpan is the HTML span element name for wrapping inline content.
	ElementSpan = "span"

	// ElementTable is the HTML table element name.
	ElementTable = "table"

	// ElementTbody is the HTML tbody element tag name.
	ElementTbody = "tbody"

	// ElementTr is the HTML element tag for a table row.
	ElementTr = "tr"

	// ElementTd is the HTML table cell element name.
	ElementTd = "td"

	// ElementUl is the HTML tag name for unordered lists.
	ElementUl = "ul"

	// WidthMobileBreakpoint is the default container width in pixels used when no
	// parent width is specified.
	WidthMobileBreakpoint = 600

	// WidthDesktopMax is the maximum width in pixels for desktop viewports.
	WidthDesktopMax = 1200

	// PercentFull is the value 100 used in percentage calculations.
	PercentFull = 100.0

	// PercentHalf is the percentage value representing one half (50%).
	PercentHalf = 0.5

	// NumericTwenty is the threshold in pixels for the largest border radius.
	NumericTwenty = 20

	// NumericFifty is the highest allowed arcsize percentage for VML button corners.
	NumericFifty = 50

	// BorderPartsMinimum is the minimum number of parts expected when parsing a
	// border string.
	BorderPartsMinimum = 3

	// FormatPercent is the suffix used to identify percentage-based width values.
	FormatPercent = "%"

	// FormatFloatCompact is the format string for compact float representation.
	FormatFloatCompact = "%g"

	// VMLPositionTopCentre is the VML position value for top centre alignment.
	VMLPositionTopCentre = "0.5,0"

	// VMLPositionBottomCentre is the VML position value for bottom centre alignment.
	VMLPositionBottomCentre = "0.5,1"

	// VMLPositionLeftCentre is the VML position for left-centre alignment.
	VMLPositionLeftCentre = "0,0.5"

	// VMLPositionRightCentre is the VML position for right-centre alignment.
	VMLPositionRightCentre = "1,0.5"

	// VMLPositionCentreCentre is the VML position value for centre alignment.
	VMLPositionCentreCentre = "0.5,0.5"

	// ConditionalCommentStart is the opening tag for Microsoft Outlook conditional
	// comments in HTML emails.
	ConditionalCommentStart = "<!--[if mso | IE]>"

	// ConditionalCommentEnd is the closing tag for Internet Explorer conditional
	// comments. It is used with VML rendering to end browser-specific blocks.
	ConditionalCommentEnd = "<![endif]-->"

	// ConditionalNotMsoStart is the HTML comment that begins a non-Outlook section.
	ConditionalNotMsoStart = "<!--[if !mso | IE]><!-->"

	// ConditionalNotMsoEnd is the closing tag for non-MSO conditional content.
	ConditionalNotMsoEnd = "<!--<![endif]-->"

	// ClassColGroup is the CSS class applied to column divs for inline-block grouping.
	ClassColGroup = "pml-col-group"

	// ClassColumnPrefix is the CSS class name prefix for column width percentages.
	ClassColumnPrefix = "pml-col-"

	// ClassListFirst is the CSS class for the first item in a list.
	ClassListFirst = "pml-list-first"

	// ClassListLast is the CSS class added to the last item in a list.
	ClassListLast = "pml-list-last"

	// ClassList is the CSS class added to list elements.
	ClassList = "pml-list"

	// ClassListWrapper is the CSS class for the wrapper div around lists.
	ClassListWrapper = "pml-list-wrapper"

	// ClassFluidMobile is the CSS class for full-width mobile styling.
	ClassFluidMobile = "pml-fluid-mobile"

	// DataPmlPadding is the data attribute for storing PML image padding values.
	DataPmlPadding = "data-pml-padding"

	// DataPmlAlign is the data attribute name for storing alignment values.
	DataPmlAlign = "data-pml-align"

	// DataPmlPaddingTop is the HTML data attribute for storing padding-top values.
	DataPmlPaddingTop = "data-pml-padding-top"

	// DataPmlPaddingRight is the data attribute for storing padding-right values.
	DataPmlPaddingRight = "data-pml-padding-right"

	// DataPmlPaddingBottom is the data attribute for storing bottom padding values.
	DataPmlPaddingBottom = "data-pml-padding-bottom"

	// DataPmlPaddingLeft is the HTML data attribute for storing left padding.
	DataPmlPaddingLeft = "data-pml-padding-left"

	// DataPmlContainerBgColor is the data attribute for container background colour.
	DataPmlContainerBgColor = "data-pml-container-background-color"

	// CSSFluidTable is the CSS rule that makes tables fill the full width on mobile.
	CSSFluidTable = "width: 100% !important;"

	// CSSFluidTd is the CSS rule that makes table cells fluid on mobile devices.
	CSSFluidTd = "width: auto !important;"

	// SelectorTableFluid is the CSS selector for fluid-width tables on mobile.
	SelectorTableFluid = "table.pml-fluid-mobile"

	// SelectorTdFluid is the CSS selector for fluid table cells on mobile devices.
	SelectorTdFluid = "td.pml-fluid-mobile"

	// defaultListMarginLeft is the left margin used for list elements when no
	// margin is set.
	defaultListMarginLeft = "25px"

	// defaultListAlign is the default horizontal alignment for list items.
	defaultListAlign = "left"

	// defaultListStyle is the default style for pml-ol list elements.
	defaultListStyle = "ordered"

	// defaultOrderedType is the default list type for ordered lists.
	defaultOrderedType = "1"

	// defaultUnorderedType is the default bullet style for unordered lists.
	defaultUnorderedType = "disc"

	// msoUlMargin removes extra margins from unordered lists in Outlook.
	msoUlMargin = "margin: 0 !important;"

	// msoListItemMarginLeft is the left margin style for list items in Outlook.
	msoListItemMarginLeft = "margin-left: 40px !important;"

	// msoFirstListItemMarginTop is the MSO style for top margin on first list items.
	msoFirstListItemMarginTop = "margin-top: 20px !important;"

	// msoLastListItemMarginBottom is the MSO style for the bottom margin of the
	// last list item.
	msoLastListItemMarginBottom = "margin-bottom: 20px !important;"

	// defaultImageAlt is the fallback alt text for images when none is given.
	defaultImageAlt = ""

	// defaultImageAlign is the default alignment setting for images.
	defaultImageAlign = "center"

	// defaultImageBorder is the default border width for images.
	defaultImageBorder = "0"

	// defaultImageHeight is the default height for images.
	defaultImageHeight = "auto"

	// defaultImagePadding is the default padding applied to images.
	defaultImagePadding = "10px 25px"

	// defaultImageTarget is the default link target for images, opening in a new tab.
	defaultImageTarget = "_blank"

	// defaultImageFontSize is the default text size for image elements.
	defaultImageFontSize = "13px"

	// defaultContainerWidth is the fallback container width in pixels when no
	// width is given.
	defaultContainerWidth = 600

	// defaultRowBackgroundRepeat is the default CSS background-repeat value for
	// Row components.
	defaultRowBackgroundRepeat = "repeat"

	// defaultRowBackgroundSize is the default CSS background-size for rows.
	defaultRowBackgroundSize = "auto"

	// defaultRowBackgroundPosition is the default background-position for rows.
	defaultRowBackgroundPosition = "top center"

	// defaultRowDirection is the default text direction for row elements.
	defaultRowDirection = "ltr"

	// defaultRowPadding is the default padding for row elements.
	defaultRowPadding = "20px 0"

	// defaultRowTextAlign is the default horizontal text alignment for rows.
	defaultRowTextAlign = "center"

	// defaultRowTextPadding is the default inner spacing for text in row elements.
	defaultRowTextPadding = "4px 4px 4px 0"
)
