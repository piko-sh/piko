/* Code generated by piko; DO NOT EDIT. */

import { ActionBuilder, createActionBuilder, registerActionFunction } from '/_piko/dist/ppframework.core.es.js';

export interface SaveInput {
  "full name": string;
  "my-kind": string;
}

export interface SaveOutput {
  saved: boolean;
  "2fa-version": string;
}

/**
 * SaveAction carries JSON tags that are not identifiers, so the generated TypeScript has
 * to quote them rather than emit them bare.
 */
export function recordSave(input: SaveInput): ActionBuilder<SaveOutput> {
  return createActionBuilder<SaveOutput>('record.Save', input);
}

export const action = {
  record: {
    Save: recordSave
  }
} as const;

registerActionFunction('record.Save', recordSave);
