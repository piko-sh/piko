// Code generated by Piko. DO NOT EDIT.

package db

import "context"

const listrecenttasks = `SELECT
    id, workflow_id, executor, status, priority, attempt,
    last_error, created_at, updated_at
FROM tasks
ORDER BY updated_at DESC
LIMIT ?;`

type ListRecentTasksRow struct {
	ID         string  `json:"id"`
	WorkflowID string  `json:"workflow_id"`
	Executor   string  `json:"executor"`
	Status     string  `json:"status"`
	Priority   int32   `json:"priority"`
	Attempt    int32   `json:"attempt"`
	LastError  *string `json:"last_error"`
	CreatedAt  int64   `json:"created_at"`
	UpdatedAt  int64   `json:"updated_at"`
}

func (queries *Queries) ListRecentTasks(ctx context.Context, limit int) ([]ListRecentTasksRow, error) {
	rows, err := queries.reader.QueryContext(ctx, listrecenttasks, limit)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var results []ListRecentTasksRow
	for rows.Next() {
		var row ListRecentTasksRow
		if err := rows.Scan(&row.ID, &row.WorkflowID, &row.Executor, &row.Status, &row.Priority, &row.Attempt, &row.LastError, &row.CreatedAt, &row.UpdatedAt); err != nil {
			return nil, err
		}
		results = append(results, row)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return results, nil
}

const listtaskstatuscounts = `SELECT status, COUNT(*) AS task_count
FROM tasks
GROUP BY status;`

type ListTaskStatusCountsRow struct {
	Status    string `json:"status"`
	TaskCount int64  `json:"task_count"`
}

func (queries *Queries) ListTaskStatusCounts(ctx context.Context) ([]ListTaskStatusCountsRow, error) {
	rows, err := queries.reader.QueryContext(ctx, listtaskstatuscounts)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var results []ListTaskStatusCountsRow
	for rows.Next() {
		var row ListTaskStatusCountsRow
		if err := rows.Scan(&row.Status, &row.TaskCount); err != nil {
			return nil, err
		}
		results = append(results, row)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return results, nil
}

const listworkflowsummary = `SELECT
    workflow_id,
    COUNT(*) AS task_count,
    SUM(CASE WHEN status = 'COMPLETE' THEN 1 ELSE 0 END) AS complete_count,
    SUM(CASE WHEN status = 'FAILED' THEN 1 ELSE 0 END) AS failed_count,
    SUM(CASE WHEN status NOT IN ('COMPLETE', 'FAILED') THEN 1 ELSE 0 END) AS active_count,
    MIN(created_at) AS created_at,
    MAX(updated_at) AS updated_at
FROM tasks
GROUP BY workflow_id
ORDER BY MAX(updated_at) DESC
LIMIT ?;`

type ListWorkflowSummaryRow struct {
	WorkflowID    string `json:"workflow_id"`
	TaskCount     int64  `json:"task_count"`
	CompleteCount *int64 `json:"complete_count"`
	FailedCount   *int64 `json:"failed_count"`
	ActiveCount   *int64 `json:"active_count"`
	CreatedAt     *int64 `json:"created_at"`
	UpdatedAt     *int64 `json:"updated_at"`
}

func (queries *Queries) ListWorkflowSummary(ctx context.Context, limit int) ([]ListWorkflowSummaryRow, error) {
	rows, err := queries.reader.QueryContext(ctx, listworkflowsummary, limit)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var results []ListWorkflowSummaryRow
	for rows.Next() {
		var row ListWorkflowSummaryRow
		if err := rows.Scan(&row.WorkflowID, &row.TaskCount, &row.CompleteCount, &row.FailedCount, &row.ActiveCount, &row.CreatedAt, &row.UpdatedAt); err != nil {
			return nil, err
		}
		results = append(results, row)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return results, nil
}
