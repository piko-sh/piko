{
  "description": "Tests that expressions in slotted content are resolved against the invoker's context, not the partial's.",
  "expectedDiagnostics": 0,
  "assertNodes": [
    {
      "select": "h1",
      "description": "The expression in the partial's template should resolve against the partial's 'state'.",
      "assertOnInterpolation": {
        "rawExpression": "state.WrapperTitle",
        "resolvedTypeIs": "string",
        "symbolNameIs": "WrapperTitle"
      }
    },
    {
      "select": "p",
      "description": "The expression in the slotted content should resolve against the invoker's 'state'.",
      "assertOnInterpolation": {
        "rawExpression": "state.MainMessage",
        "resolvedTypeIs": "string",
        "symbolNameIs": "MainMessage"
      }
    }
  ]
}