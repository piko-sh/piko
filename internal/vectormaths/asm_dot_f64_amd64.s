// Code generated by cmd/asmgen; DO NOT EDIT.

// Copyright 2026 PolitePixels Limited
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     http://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

// This project stands against fascism, authoritarianism, and all forms of
// oppression. We built this to empower people, not to enable those who would
// strip others of their rights and dignity.

//go:build !safe && !(js && wasm) && amd64

#include "textflag.h"

// dotF64SSE computes sum(a[i]*b[i]) for f64 vectors using 4-way SSE2 unrolling.
TEXT ·dotF64SSE(SB), NOSPLIT, $0-56
	MOVQ    a_base+0(FP), SI
	MOVQ    a_len+8(FP), CX
	MOVQ    b_base+24(FP), DI

	XORPD   X0, X0
	XORPD   X4, X4
	XORPD   X5, X5
	XORPD   X6, X6
	XORPD   X3, X3

	CMPQ    CX, $8
	JL      dotf64sse_tail2

dotf64sse_loop8:
	MOVUPD  0(SI), X1
	MOVUPD  0(DI), X2
	MULPD   X2, X1
	ADDPD   X1, X0

	MOVUPD  16(SI), X1
	MOVUPD  16(DI), X2
	MULPD   X2, X1
	ADDPD   X1, X4

	MOVUPD  32(SI), X1
	MOVUPD  32(DI), X2
	MULPD   X2, X1
	ADDPD   X1, X5

	MOVUPD  48(SI), X1
	MOVUPD  48(DI), X2
	MULPD   X2, X1
	ADDPD   X1, X6

	ADDQ    $64, SI
	ADDQ    $64, DI
	SUBQ    $8, CX
	CMPQ    CX, $8
	JGE     dotf64sse_loop8

	ADDPD   X4, X0
	ADDPD   X5, X0
	ADDPD   X6, X0

dotf64sse_tail2:
	CMPQ    CX, $2
	JL      dotf64sse_tail

dotf64sse_loop2:
	MOVUPD  (SI), X1
	MOVUPD  (DI), X2
	MULPD   X2, X1
	ADDPD   X1, X0
	ADDQ    $16, SI
	ADDQ    $16, DI
	SUBQ    $2, CX
	CMPQ    CX, $2
	JGE     dotf64sse_loop2

dotf64sse_tail:
	TESTQ   CX, CX
	JZ      dotf64sse_reduce

dotf64sse_tail_loop:
	MOVSD   (SI), X1
	MOVSD   (DI), X2
	MULSD   X2, X1
	ADDSD   X1, X3
	ADDQ    $8, SI
	ADDQ    $8, DI
	DECQ    CX
	JNZ     dotf64sse_tail_loop

dotf64sse_reduce:
	MOVAPD  X0, X1
	UNPCKHPD X0, X1
	ADDSD   X1, X0
	ADDSD   X3, X0
	MOVSD   X0, ret+48(FP)
	RET

// dotF64AVX2 computes sum(a[i]*b[i]) for f64 vectors using 8-way AVX2 unrolling.
TEXT ·dotF64AVX2(SB), NOSPLIT, $0-56
	MOVQ    a_base+0(FP), SI
	MOVQ    a_len+8(FP), CX
	MOVQ    b_base+24(FP), DI

	VXORPD  Y0, Y0, Y0
	VXORPD  Y4, Y4, Y4
	VXORPD  Y5, Y5, Y5
	VXORPD  Y6, Y6, Y6
	VXORPD  Y7, Y7, Y7
	VXORPD  Y8, Y8, Y8
	VXORPD  Y9, Y9, Y9
	VXORPD  Y10, Y10, Y10
	VXORPD  X3, X3, X3

	CMPQ    CX, $32
	JL      dotf64avx_tail4

dotf64avx_loop32:
	VMOVUPD 0(SI), Y1
	VMOVUPD 0(DI), Y2
	VFMADD231PD Y1, Y2, Y0

	VMOVUPD 32(SI), Y1
	VMOVUPD 32(DI), Y2
	VFMADD231PD Y1, Y2, Y4

	VMOVUPD 64(SI), Y1
	VMOVUPD 64(DI), Y2
	VFMADD231PD Y1, Y2, Y5

	VMOVUPD 96(SI), Y1
	VMOVUPD 96(DI), Y2
	VFMADD231PD Y1, Y2, Y6

	VMOVUPD 128(SI), Y1
	VMOVUPD 128(DI), Y2
	VFMADD231PD Y1, Y2, Y7

	VMOVUPD 160(SI), Y1
	VMOVUPD 160(DI), Y2
	VFMADD231PD Y1, Y2, Y8

	VMOVUPD 192(SI), Y1
	VMOVUPD 192(DI), Y2
	VFMADD231PD Y1, Y2, Y9

	VMOVUPD 224(SI), Y1
	VMOVUPD 224(DI), Y2
	VFMADD231PD Y1, Y2, Y10

	ADDQ    $256, SI
	ADDQ    $256, DI
	SUBQ    $32, CX
	CMPQ    CX, $32
	JGE     dotf64avx_loop32

	VADDPD  Y4, Y0, Y0
	VADDPD  Y5, Y0, Y0
	VADDPD  Y6, Y0, Y0
	VADDPD  Y7, Y0, Y0
	VADDPD  Y8, Y0, Y0
	VADDPD  Y9, Y0, Y0
	VADDPD  Y10, Y0, Y0

dotf64avx_tail4:
	CMPQ    CX, $4
	JL      dotf64avx_tail

dotf64avx_loop4:
	VMOVUPD (SI), Y1
	VMOVUPD (DI), Y2
	VFMADD231PD Y1, Y2, Y0
	ADDQ    $32, SI
	ADDQ    $32, DI
	SUBQ    $4, CX
	CMPQ    CX, $4
	JGE     dotf64avx_loop4

dotf64avx_tail:
	TESTQ   CX, CX
	JZ      dotf64avx_reduce

dotf64avx_tail_loop:
	VMOVSD  (SI), X1
	VMOVSD  (DI), X2
	VMULSD  X2, X1, X1
	VADDSD  X1, X3, X3
	ADDQ    $8, SI
	ADDQ    $8, DI
	DECQ    CX
	JNZ     dotf64avx_tail_loop

dotf64avx_reduce:
	VEXTRACTF128 $1, Y0, X1
	VADDPD  X1, X0, X0
	VSHUFPD $1, X0, X0, X1
	VADDSD  X1, X0, X0
	VADDSD  X3, X0, X0
	VZEROUPPER
	MOVSD   X0, ret+48(FP)
	RET
