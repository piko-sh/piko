// Code generated by cmd/asmgen; DO NOT EDIT.

// Copyright 2026 PolitePixels Limited
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     http://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

// This project stands against fascism, authoritarianism, and all forms of
// oppression. We built this to empower people, not to enable those who would
// strip others of their rights and dignity.

//go:build !safe && !(js && wasm) && amd64

#include "textflag.h"

// addF64SSE computes dst[i] = a[i] + b[i] for f64 vectors using 2-way SSE2 unrolling.
TEXT ·addF64SSE(SB), NOSPLIT, $0-72
	MOVQ    dst_base+0(FP), DI
	MOVQ    dst_len+8(FP), CX
	MOVQ    a_base+24(FP), SI
	MOVQ    b_base+48(FP), BX

	CMPQ    CX, $4
	JL      addf64sse_tail2

addf64sse_loop4:
	MOVUPD  0(SI), X0
	MOVUPD  0(BX), X1
	ADDPD   X1, X0
	MOVUPD  X0, 0(DI)

	MOVUPD  16(SI), X2
	MOVUPD  16(BX), X3
	ADDPD   X3, X2
	MOVUPD  X2, 16(DI)

	ADDQ    $32, SI
	ADDQ    $32, BX
	ADDQ    $32, DI
	SUBQ    $4, CX
	CMPQ    CX, $4
	JGE     addf64sse_loop4

addf64sse_tail2:
	CMPQ    CX, $2
	JL      addf64sse_tail

	MOVUPD  (SI), X0
	MOVUPD  (BX), X1
	ADDPD   X1, X0
	MOVUPD  X0, (DI)
	ADDQ    $16, SI
	ADDQ    $16, BX
	ADDQ    $16, DI
	SUBQ    $2, CX

addf64sse_tail:
	TESTQ   CX, CX
	JZ      addf64sse_done

addf64sse_tail_loop:
	MOVSD   (SI), X0
	MOVSD   (BX), X1
	ADDSD   X1, X0
	MOVSD   X0, (DI)
	ADDQ    $8, SI
	ADDQ    $8, BX
	ADDQ    $8, DI
	DECQ    CX
	JNZ     addf64sse_tail_loop

addf64sse_done:
	RET

// addF64AVX2 computes dst[i] = a[i] + b[i] for f64 vectors using 4-way AVX2 unrolling.
TEXT ·addF64AVX2(SB), NOSPLIT, $0-72
	MOVQ    dst_base+0(FP), DI
	MOVQ    a_base+24(FP), SI
	MOVQ    a_len+32(FP), CX
	MOVQ    b_base+48(FP), BX

	CMPQ    CX, $16
	JL      addf64avx_tail4

addf64avx_loop16:
	VMOVUPD 0(SI), Y0
	VADDPD  0(BX), Y0, Y0
	VMOVUPD Y0, 0(DI)

	VMOVUPD 32(SI), Y0
	VADDPD  32(BX), Y0, Y0
	VMOVUPD Y0, 32(DI)

	VMOVUPD 64(SI), Y0
	VADDPD  64(BX), Y0, Y0
	VMOVUPD Y0, 64(DI)

	VMOVUPD 96(SI), Y0
	VADDPD  96(BX), Y0, Y0
	VMOVUPD Y0, 96(DI)

	ADDQ    $128, SI
	ADDQ    $128, BX
	ADDQ    $128, DI
	SUBQ    $16, CX
	CMPQ    CX, $16
	JGE     addf64avx_loop16

addf64avx_tail4:
	CMPQ    CX, $4
	JL      addf64avx_tail2

addf64avx_loop4:
	VMOVUPD (SI), Y0
	VADDPD  (BX), Y0, Y0
	VMOVUPD Y0, (DI)
	ADDQ    $32, SI
	ADDQ    $32, BX
	ADDQ    $32, DI
	SUBQ    $4, CX
	CMPQ    CX, $4
	JGE     addf64avx_loop4

addf64avx_tail2:
	CMPQ    CX, $2
	JL      addf64avx_tail1

	MOVUPD  (SI), X0
	MOVUPD  (BX), X1
	ADDPD   X1, X0
	MOVUPD  X0, (DI)
	ADDQ    $16, SI
	ADDQ    $16, BX
	ADDQ    $16, DI
	SUBQ    $2, CX

addf64avx_tail1:
	TESTQ   CX, CX
	JZ      addf64avx_done

addf64avx_tail_loop:
	VMOVSD  (SI), X0
	VADDSD  (BX), X0, X0
	VMOVSD  X0, (DI)
	ADDQ    $8, SI
	ADDQ    $8, BX
	ADDQ    $8, DI
	DECQ    CX
	JNZ     addf64avx_tail_loop

addf64avx_done:
	VZEROUPPER
	RET
