// Code generated by cmd/asmgen; DO NOT EDIT.

// Copyright 2026 PolitePixels Limited
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     http://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

// This project stands against fascism, authoritarianism, and all forms of
// oppression. We built this to empower people, not to enable those who would
// strip others of their rights and dignity.

//go:build !safe && !(js && wasm) && amd64

#include "textflag.h"

// sumF64SSE computes sum(a[i]) for f64 vectors using 4-way SSE2 unrolling.
TEXT ·sumF64SSE(SB), NOSPLIT, $0-32
	MOVQ    a_base+0(FP), SI
	MOVQ    a_len+8(FP), CX

	XORPD   X0, X0
	XORPD   X4, X4
	XORPD   X5, X5
	XORPD   X6, X6
	XORPD   X3, X3

	CMPQ    CX, $8
	JL      sumf64sse_tail2

sumf64sse_loop8:
	MOVUPD  0(SI), X1
	ADDPD   X1, X0
	MOVUPD  16(SI), X1
	ADDPD   X1, X4
	MOVUPD  32(SI), X1
	ADDPD   X1, X5
	MOVUPD  48(SI), X1
	ADDPD   X1, X6
	ADDQ    $64, SI
	SUBQ    $8, CX
	CMPQ    CX, $8
	JGE     sumf64sse_loop8

	ADDPD   X4, X0
	ADDPD   X5, X0
	ADDPD   X6, X0

sumf64sse_tail2:
	CMPQ    CX, $2
	JL      sumf64sse_tail

sumf64sse_loop2:
	MOVUPD  (SI), X1
	ADDPD   X1, X0
	ADDQ    $16, SI
	SUBQ    $2, CX
	CMPQ    CX, $2
	JGE     sumf64sse_loop2

sumf64sse_tail:
	TESTQ   CX, CX
	JZ      sumf64sse_reduce

sumf64sse_tail_loop:
	MOVSD   (SI), X1
	ADDSD   X1, X3
	ADDQ    $8, SI
	DECQ    CX
	JNZ     sumf64sse_tail_loop

sumf64sse_reduce:
	MOVAPD  X0, X1
	UNPCKHPD X0, X1
	ADDSD   X1, X0
	ADDSD   X3, X0
	MOVSD   X0, ret+24(FP)
	RET

// sumF64AVX2 computes sum(a[i]) for f64 vectors using 8-way AVX2 unrolling.
TEXT ·sumF64AVX2(SB), NOSPLIT, $0-32
	MOVQ    a_base+0(FP), SI
	MOVQ    a_len+8(FP), CX

	VXORPD  Y0, Y0, Y0
	VXORPD  Y4, Y4, Y4
	VXORPD  Y5, Y5, Y5
	VXORPD  Y6, Y6, Y6
	VXORPD  Y7, Y7, Y7
	VXORPD  Y8, Y8, Y8
	VXORPD  Y9, Y9, Y9
	VXORPD  Y10, Y10, Y10
	VXORPD  X3, X3, X3

	CMPQ    CX, $32
	JL      sumf64avx_tail4

sumf64avx_loop32:
	VMOVUPD 0(SI), Y1
	VADDPD  Y1, Y0, Y0
	VMOVUPD 32(SI), Y1
	VADDPD  Y1, Y4, Y4
	VMOVUPD 64(SI), Y1
	VADDPD  Y1, Y5, Y5
	VMOVUPD 96(SI), Y1
	VADDPD  Y1, Y6, Y6
	VMOVUPD 128(SI), Y1
	VADDPD  Y1, Y7, Y7
	VMOVUPD 160(SI), Y1
	VADDPD  Y1, Y8, Y8
	VMOVUPD 192(SI), Y1
	VADDPD  Y1, Y9, Y9
	VMOVUPD 224(SI), Y1
	VADDPD  Y1, Y10, Y10
	ADDQ    $256, SI
	SUBQ    $32, CX
	CMPQ    CX, $32
	JGE     sumf64avx_loop32

	VADDPD  Y4, Y0, Y0
	VADDPD  Y5, Y0, Y0
	VADDPD  Y6, Y0, Y0
	VADDPD  Y7, Y0, Y0
	VADDPD  Y8, Y0, Y0
	VADDPD  Y9, Y0, Y0
	VADDPD  Y10, Y0, Y0

sumf64avx_tail4:
	CMPQ    CX, $4
	JL      sumf64avx_tail

sumf64avx_loop4:
	VMOVUPD (SI), Y1
	VADDPD  Y1, Y0, Y0
	ADDQ    $32, SI
	SUBQ    $4, CX
	CMPQ    CX, $4
	JGE     sumf64avx_loop4

sumf64avx_tail:
	TESTQ   CX, CX
	JZ      sumf64avx_reduce

sumf64avx_tail_loop:
	VMOVSD  (SI), X1
	VADDSD  X1, X3, X3
	ADDQ    $8, SI
	DECQ    CX
	JNZ     sumf64avx_tail_loop

sumf64avx_reduce:
	VEXTRACTF128 $1, Y0, X1
	VADDPD  X1, X0, X0
	VSHUFPD $1, X0, X0, X1
	VADDSD  X1, X0, X0
	VADDSD  X3, X0, X0
	VZEROUPPER
	MOVSD   X0, ret+24(FP)
	RET
