<template>
  <div class="site">
    <header class="site-header">
      <div class="container">
        <piko:a href="/" class="site-title">Piko blog</piko:a>
      </div>
    </header>

    <main class="site-main">
      <div class="container">
        <piko:slot />
      </div>
    </main>

    <footer class="site-footer">
      <div class="container">
        <p class="site-copyright">&copy; 2026 Piko blog. Built with Piko.</p>
      </div>
    </footer>
  </div>
</template>

<script type="application/x-go">
package main

import "piko.sh/piko"

type Response struct{}

func Render(r *piko.RequestData, props piko.NoProps) (Response, piko.Metadata, error) {
    return Response{}, piko.Metadata{}, nil
}
</script>

<style>
  @import "testmodule/styles/theme.css";

  body {
    margin: 0;
  }

  .site {
    min-height: 100vh;
    display: flex;
    flex-direction: column;
    font-family: var(--g-font-family, sans-serif);
    background:
      radial-gradient(ellipse 80% 50% at 50% -20%, rgba(111, 71, 235, 0.07), transparent),
      var(--g-colour-grey-50, #F6F7F8);
    color: var(--g-colour-grey-800, #364049);
    line-height: 1.6;
  }

  .container {
    max-width: 42rem;
    margin: 0 auto;
    padding: 0 1.5rem;
    width: 100%;
  }

  .site-header {
    border-bottom: 1px solid var(--g-colour-grey-100, #E8EBEE);
    padding: 1rem 0;
  }

  .site-title {
    font-size: 1.25rem;
    font-family: var(--g-font-family-heading), sans-serif;
    font-weight: 400;
    color: var(--g-colour-grey-600);
    text-decoration: none;
    letter-spacing: -0.01em;
    transition: color .2s;
  }

  .site-title:hover {
    color: var(--g-colour-primary-600, #5A39C7);
  }

  .site-main {
    flex: 1;
    padding: 2rem 0;
  }

  .site-footer {
    border-top: 1px solid var(--g-colour-grey-100, #E8EBEE);
    padding: 1.5rem 0;
    margin-top: auto;
  }

  .site-copyright {
    font-size: 0.875rem;
    color: var(--g-colour-grey-500, #788A9B);
    margin: 0;
  }
</style>
