<template>
  <div id="app">
    <h1>Todo app</h1>
    <p>An interactive todo list built with PKC.</p>
    <pp-todo-list></pp-todo-list>
  </div>
</template>

<!-- Hosts the pp-todo-list PKC component. The component handles all
     state and interaction in the browser via Shadow DOM.
     See https://piko.sh/docs/guide/client-components -->
<script type="application/x-go">
package main

import "piko.sh/piko"

func Render(r *piko.RequestData, props piko.NoProps) (piko.NoResponse, piko.Metadata, error) {
    return piko.NoResponse{}, piko.Metadata{
        Title: "Todo app — Piko example",
    }, nil
}
</script>

<style>
  @import "testmodule/styles/theme.css";

  #app {
    max-width: 600px;
    margin: 2rem auto;
    padding: 1rem;
    font-family: var(--g-font-family, sans-serif);
    background-color: var(--g-colour-grey-50, #F6F7F8);
    color: var(--g-colour-grey-800, #364049);
    text-align: center;
    border-radius: var(--g-radius-xl);
  }

  h1 {
    font-family: var(--g-font-family-heading);
    color: var(--g-colour-grey-900, #2B333B);
    letter-spacing: -0.01em;
    font-size: 2rem;
    margin-bottom: .5em;
  }

  p {
    color: var(--g-colour-grey-500, #788A9B);
    margin-bottom: 2rem;
  }
</style>
