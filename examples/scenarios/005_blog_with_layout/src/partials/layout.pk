<!-- Reusable layout partial with nested navigation, a default <piko:slot> for
     main content, and a named "sidebar" slot with fallback content.
     See https://piko.sh/docs/guide/partials -->
<template>
  <div class="layout">
    <header class="layout-header">
      <piko:partial is="nav" :server.current-page="props.CurrentPage"></piko:partial>
    </header>

    <div class="layout-body">
      <main class="layout-main">
        <h1 p-text="props.Title"></h1>
        <p class="layout-description" p-if="props.Description != ''" p-text="props.Description"></p>
        <piko:slot></piko:slot>
      </main>

      <aside class="layout-sidebar">
        <piko:slot name="sidebar">
          <p class="sidebar-fallback">No sidebar content provided.</p>
        </piko:slot>
      </aside>
    </div>

    <footer class="layout-footer">
      <p>Built with Piko. All rights reserved.</p>
    </footer>
  </div>
</template>

<script type="application/x-go">
package main

import (
  "piko.sh/piko"
  nav "testmodule/partials/navigation.pk"
)

type Props struct {
  Title       string `prop:"title"`
  Description string `prop:"description"`
  CurrentPage string `prop:"current-page"`
}

func Render(r *piko.RequestData, props Props) (piko.NoResponse, piko.Metadata, error) {
  return piko.NoResponse{}, piko.Metadata{}, nil
}
</script>

<style>
  @import "testmodule/styles/theme.css";

  .layout {
    --nav-bg: var(--g-colour-grey-25, #fafbfc);
    --content-max-width: 1200px;
    --sidebar-width: 300px;

    min-height: 100vh;
    display: flex;
    flex-direction: column;
    font-family: var(--g-font-family, sans-serif);
    background:
      radial-gradient(ellipse 80% 50% at 50% -20%, rgba(111, 71, 235, 0.07), transparent),
      var(--g-colour-grey-50, #F6F7F8);
    color: var(--g-colour-grey-800, #364049);
  }

  .layout-header {
    flex-shrink: 0;
  }

  .layout-body {
    display: flex;
    max-width: var(--content-max-width);
    margin: 0 auto;
    padding: 2rem;
    gap: 2rem;
    flex-grow: 1;
    width: 100%;
  }

  .layout-main {
    flex-grow: 1;
    min-width: 0;
  }

  .layout-main h1 {
    font-family: var(--g-font-family-heading, sans-serif);
    font-size: 2.75rem;
    font-weight: 500;
    margin-bottom: 0.5rem;
    color: var(--g-colour-grey-900, #2B333B);
  }

  .layout-description {
    color: var(--g-colour-grey-500, #788A9B);
    font-size: 1.25rem;
    margin-bottom: 2rem;
    line-height: 1.6;
  }

  .layout-sidebar {
    flex-shrink: 0;
    width: var(--sidebar-width);
    border-left: 1px solid var(--g-colour-grey-100, #E8EBEE);
    padding-left: 2rem;
  }

  .sidebar-fallback {
    color: var(--g-colour-grey-400, #92A1AF);
    font-style: italic;
  }

  .layout-footer {
    flex-shrink: 0;
    padding: 1.5rem 2rem;
    background-color: var(--g-colour-grey-50, #F6F7F8);
    border-top: 1px solid var(--g-colour-grey-100, #E8EBEE);
    text-align: center;
  }

  .layout-footer p {
    color: var(--g-colour-grey-400, #92A1AF);
    font-size: 0.875rem;
    margin: 0;
  }

  @media (max-width: 768px) {
    .layout-body {
      flex-direction: column;
      padding: 1.5rem 1rem;
      gap: 1.5rem;
    }

    .layout-main h1 {
      font-size: 2rem;
    }

    .layout-description {
      font-size: 1.0625rem;
      margin-bottom: 1.5rem;
    }

    .layout-sidebar {
      width: 100%;
      border-left: none;
      padding-left: 0;
      border-top: 1px solid var(--g-colour-grey-100, #E8EBEE);
      padding-top: 1.5rem;
    }

    .layout-footer {
      padding: 1.25rem 1rem;
    }
  }
</style>
