<template>
    <div id="app">
        <h1 class="page-title">Built-in components</h1>
        <p class="page-intro">Demonstrating Piko's built-in components loaded via <code>components.Piko()</code>.</p>

        <h2 class="section-heading">piko-counter</h2>
        <piko-counter></piko-counter>

        <h2 class="section-heading">piko-card</h2>
        <piko-card>
            <span slot="header">Hello from a built-in card</span>
            <p>This card component ships with the Piko framework and is loaded from an embedded filesystem.</p>
            <span slot="footer">Piko built-in components</span>
        </piko-card>
    </div>
</template>

<script type="application/x-go">
package main

import "piko.sh/piko"

func Render(r *piko.RequestData, props piko.NoProps) (piko.NoResponse, piko.Metadata, error) {
    return piko.NoResponse{}, piko.Metadata{
        Title: "Built-in components | Piko example",
    }, nil
}
</script>

<style>
    @import "testmodule/styles/theme.css";

    #app {
        max-width: 600px;
        margin: 2rem auto;
        padding: 1rem;
        border-radius: var(--g-radius-xl, 30px);
        font-family: var(--g-font-family, sans-serif);
        background:
            radial-gradient(ellipse 80% 50% at 50% -20%, rgba(111, 71, 235, 0.07), transparent),
            var(--g-colour-grey-50, #F6F7F8);
        color: var(--g-colour-grey-800, #364049);
    }

    .page-title {
        color: var(--g-colour-grey-900, #2B333B);
        font-family: var(--g-font-family-heading, sans-serif);
        font-size: 2rem;
        margin-bottom: 0.5rem;
    }

    .section-heading {
        color: var(--g-colour-grey-900, #2B333B);
        margin-top: 2rem;
        font-weight: 500;
        margin-bottom: .5em;
        font-size: 1.25rem;
    }

    .page-intro {
        color: var(--g-colour-grey-500, #788A9B);
        margin-bottom: 1rem;
    }

    code {
        background: var(--g-colour-grey-100, #E8EBEE);
        color: var(--g-colour-grey-800, #364049);
        padding: 0.125rem 0.375rem;
        border-radius: var(--g-radius-sm, 10px);
        font-family: var(--g-font-family-mono, monospace);
        font-size: 0.875rem;
    }
</style>
