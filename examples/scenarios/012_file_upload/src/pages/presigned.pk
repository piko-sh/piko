<!-- Presigned URL upload: request a presigned PUT URL from the server, upload
     directly to S3 with progress, then finalise. -->
<template>
  <div id="app">
    <h1>S3 file upload</h1>
    <piko:partial is="upload_nav" server.active="presigned"></piko:partial>

    <p class="intro">Upload files via presigned S3 URLs with progress tracking.</p>

    <piko:partial is="upload_form" server.mode="presigned" server.button-label="Upload via presigned URL"></piko:partial>

    <piko:partial is="file_list"></piko:partial>
  </div>
</template>

<script type="application/x-go">
package main

import (
    "piko.sh/piko"

    upload_form "testmodule/partials/upload-form.pk"
    upload_nav "testmodule/partials/upload-nav.pk"
    file_list "testmodule/partials/file-list.pk"
)

func Render(r *piko.RequestData, props piko.NoProps) (piko.NoResponse, piko.Metadata, error) {
    return piko.NoResponse{}, piko.Metadata{
        Title: "S3 file upload | presigned URL",
    }, nil
}
</script>

<style>
  @import "testmodule/styles/theme.css";

  * { box-sizing: border-box; }

  #app {
    max-width: 720px;
    margin: 2rem auto;
    padding: 0 1rem;
    font-family: var(--g-font-family, sans-serif);
    color: var(--g-colour-grey-800, #364049);
  }

  h1 {
    font-family: var(--g-font-family-heading);
    font-size: 2rem;
    color: var(--g-colour-grey-900, #2B333B);
    letter-spacing: -0.01em;
    margin-bottom: 1em;
  }

  .intro { color: var(--g-colour-grey-500, #788A9B); margin-bottom: 1rem; }
</style>
