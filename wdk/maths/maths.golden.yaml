# Piko Public API Surface

# This file is auto-generated by `go test -update`. DO NOT EDIT.
# It is a snapshot of the public API to prevent unintentional breaking changes.
AbsSumBigInts: func(...maths.BigInt) maths.BigInt # Kind: Func
AbsSumDecimals: func(...maths.Decimal) maths.Decimal # Kind: Func
AbsSumMoney: func(...maths.Money) maths.Money # Kind: Func
AverageBigInts: func(...maths.BigInt) maths.Decimal # Kind: Func
AverageDecimals: func(...maths.Decimal) maths.Decimal # Kind: Func
AverageMoney: func(...maths.Money) maths.Money # Kind: Func
BigInt: |- # Kind: Struct
  struct {
      // (+2 unexported fields)

      // Method
      Abs() maths.BigInt
      // Method
      Add(maths.BigInt) maths.BigInt
      // Method
      AddDecimal(maths.Decimal) maths.Decimal
      // Method
      AddFloat(float64) maths.Decimal
      // Method
      AddInPlace(maths.BigInt)
      // Method
      AddInt(int64) maths.BigInt
      // Method
      AddPercent(maths.Decimal) maths.Decimal
      // Method
      AddPercentFloat(float64) maths.Decimal
      // Method
      AddPercentInt(int64) maths.Decimal
      // Method
      AddPercentString(string) maths.Decimal
      // Method
      AddString(string) maths.BigInt
      // Method
      Allocate(...int64) ([]maths.BigInt, error)
      // Method
      AsPercentOf(maths.BigInt) maths.Decimal
      // Method
      AsPercentOfInt(int64) maths.Decimal
      // Method
      AsPercentOfString(string) maths.Decimal
      // Method
      Ceil() maths.BigInt
      // Method
      CheckEquals(maths.BigInt) bool
      // Method
      CheckGreaterThan(maths.BigInt) bool
      // Method
      CheckIsBetween(maths.BigInt, maths.BigInt) bool
      // Method
      CheckIsEven() bool
      // Method
      CheckIsInteger() bool
      // Method
      CheckIsMultipleOf(maths.BigInt) bool
      // Method
      CheckIsNegative() bool
      // Method
      CheckIsOdd() bool
      // Method
      CheckIsPositive() bool
      // Method
      CheckIsZero() bool
      // Method
      CheckLessThan(maths.BigInt) bool
      // Method
      Cmp(maths.BigInt) (int, error)
      // Method
      Divide(maths.BigInt) maths.BigInt
      // Method
      DivideDecimal(maths.Decimal) maths.Decimal
      // Method
      DivideFloat(float64) maths.Decimal
      // Method
      DivideInt(int64) maths.BigInt
      // Method
      DivideString(string) maths.BigInt
      // Method
      Equals(maths.BigInt) (bool, error)
      // Method
      EqualsInt(int64) (bool, error)
      // Method
      EqualsString(string) (bool, error)
      // Method
      Err() error
      // Method
      Float64() (float64, error)
      // Method
      Floor() maths.BigInt
      // Method
      GetPercent(maths.Decimal) maths.Decimal
      // Method
      GetPercentFloat(float64) maths.Decimal
      // Method
      GetPercentInt(int64) maths.Decimal
      // Method
      GetPercentString(string) maths.Decimal
      // Method
      GreaterThan(maths.BigInt) (bool, error)
      // Method
      GreaterThanInt(int64) (bool, error)
      // Method
      GreaterThanString(string) (bool, error)
      // Method
      Int64() (int64, error)
      // Method
      IsBetween(maths.BigInt, maths.BigInt) (bool, error)
      // Method
      IsEven() (bool, error)
      // Method
      IsInteger() (bool, error)
      // Method
      IsMultipleOf(maths.BigInt) (bool, error)
      // Method
      IsNegative() (bool, error)
      // Method
      IsOdd() (bool, error)
      // Method
      IsPositive() (bool, error)
      // Method
      IsZero() (bool, error)
      // Method
      LessThan(maths.BigInt) (bool, error)
      // Method
      LessThanInt(int64) (bool, error)
      // Method
      LessThanString(string) (bool, error)
      // Method
      MarshalJSON() ([]uint8, error)
      // Method
      Modulus(maths.BigInt) maths.BigInt
      // Method
      ModulusInt(int64) maths.BigInt
      // Method
      ModulusString(string) maths.BigInt
      // Method
      Multiply(maths.BigInt) maths.BigInt
      // Method
      MultiplyDecimal(maths.Decimal) maths.Decimal
      // Method
      MultiplyFloat(float64) maths.Decimal
      // Method
      MultiplyInPlace(maths.BigInt)
      // Method
      MultiplyInt(int64) maths.BigInt
      // Method
      MultiplyString(string) maths.BigInt
      // Method
      Must() maths.BigInt
      // Method
      MustEquals(maths.BigInt) bool
      // Method
      MustFloat64() float64
      // Method
      MustGreaterThan(maths.BigInt) bool
      // Method
      MustInt64() int64
      // Method
      MustIsBetween(maths.BigInt, maths.BigInt) bool
      // Method
      MustIsEven() bool
      // Method
      MustIsInteger() bool
      // Method
      MustIsMultipleOf(maths.BigInt) bool
      // Method
      MustIsNegative() bool
      // Method
      MustIsOdd() bool
      // Method
      MustIsPositive() bool
      // Method
      MustIsZero() bool
      // Method
      MustLessThan(maths.BigInt) bool
      // Method
      MustString() string
      // Method
      Negate() maths.BigInt
      // Method
      Power(maths.BigInt) maths.BigInt
      // Method
      PowerDecimal(maths.Decimal) maths.Decimal
      // Method
      PowerFloat(float64) maths.Decimal
      // Method
      PowerInt(int64) maths.BigInt
      // Method
      PowerString(string) maths.BigInt
      // Method
      Remainder(maths.BigInt) maths.BigInt
      // Method
      RemainderInt(int64) maths.BigInt
      // Method
      RemainderString(string) maths.BigInt
      // Method
      Round(int32) maths.BigInt
      // Method
      Scan(interface {}) error
      // Method
      String() (string, error)
      // Method
      Subtract(maths.BigInt) maths.BigInt
      // Method
      SubtractDecimal(maths.Decimal) maths.Decimal
      // Method
      SubtractFloat(float64) maths.Decimal
      // Method
      SubtractInPlace(maths.BigInt)
      // Method
      SubtractInt(int64) maths.BigInt
      // Method
      SubtractPercent(maths.Decimal) maths.Decimal
      // Method
      SubtractPercentFloat(float64) maths.Decimal
      // Method
      SubtractPercentInt(int64) maths.Decimal
      // Method
      SubtractPercentString(string) maths.Decimal
      // Method
      SubtractString(string) maths.BigInt
      // Method
      ToDecimal() maths.Decimal
      // Method
      Truncate() maths.BigInt
      // Method
      UnmarshalJSON([]uint8) error
      // Method
      Value() (driver.Value, error)
      // Method
      When(bool, func(maths.BigInt) maths.BigInt) maths.BigInt
      // Method
      WhenBetween(maths.BigInt, maths.BigInt, func(maths.BigInt) maths.BigInt) maths.BigInt
      // Method
      WhenEven(func(maths.BigInt) maths.BigInt) maths.BigInt
      // Method
      WhenInteger(func(maths.BigInt) maths.BigInt) maths.BigInt
      // Method
      WhenMultipleOf(maths.BigInt, func(maths.BigInt) maths.BigInt) maths.BigInt
      // Method
      WhenNegative(func(maths.BigInt) maths.BigInt) maths.BigInt
      // Method
      WhenOdd(func(maths.BigInt) maths.BigInt) maths.BigInt
      // Method
      WhenPositive(func(maths.BigInt) maths.BigInt) maths.BigInt
      // Method
      WhenZero(func(maths.BigInt) maths.BigInt) maths.BigInt
  }
Converter: |- # Kind: Struct
  struct {
      // (+1 unexported fields)

      // Method
      Convert(maths.Money, string) maths.Money
  }
CurrencyDefinition: |- # Kind: Struct
  struct {
      // Field
      NumericCode string
      // Field
      Digits uint8
      // Field
      DefaultSymbol string
  }
Decimal: |- # Kind: Struct
  struct {
      // (+2 unexported fields)

      // Method
      Abs() maths.Decimal
      // Method
      Add(maths.Decimal) maths.Decimal
      // Method
      AddBigInt(maths.BigInt) maths.Decimal
      // Method
      AddDecimal(maths.Decimal) maths.Decimal
      // Method
      AddFloat(float64) maths.Decimal
      // Method
      AddInPlace(maths.Decimal)
      // Method
      AddInt(int64) maths.Decimal
      // Method
      AddPercent(maths.Decimal) maths.Decimal
      // Method
      AddPercentFloat(float64) maths.Decimal
      // Method
      AddPercentInt(int64) maths.Decimal
      // Method
      AddPercentString(string) maths.Decimal
      // Method
      AddString(string) maths.Decimal
      // Method
      Allocate(...int64) ([]maths.Decimal, error)
      // Method
      AsPercentOf(maths.Decimal) maths.Decimal
      // Method
      AsPercentOfFloat(float64) maths.Decimal
      // Method
      AsPercentOfInt(int64) maths.Decimal
      // Method
      AsPercentOfString(string) maths.Decimal
      // Method
      Ceil() maths.Decimal
      // Method
      CheckEquals(maths.Decimal) bool
      // Method
      CheckGreaterThan(maths.Decimal) bool
      // Method
      CheckIsBetween(maths.Decimal, maths.Decimal) bool
      // Method
      CheckIsCloseTo(maths.Decimal, maths.Decimal) bool
      // Method
      CheckIsEven() bool
      // Method
      CheckIsInteger() bool
      // Method
      CheckIsMultipleOf(maths.Decimal) bool
      // Method
      CheckIsNegative() bool
      // Method
      CheckIsOdd() bool
      // Method
      CheckIsPositive() bool
      // Method
      CheckIsZero() bool
      // Method
      CheckLessThan(maths.Decimal) bool
      // Method
      Divide(maths.Decimal) maths.Decimal
      // Method
      DivideBigInt(maths.BigInt) maths.Decimal
      // Method
      DivideDecimal(maths.Decimal) maths.Decimal
      // Method
      DivideFloat(float64) maths.Decimal
      // Method
      DivideInt(int64) maths.Decimal
      // Method
      DivideString(string) maths.Decimal
      // Method
      Equals(maths.Decimal) (bool, error)
      // Method
      EqualsFloat(float64) (bool, error)
      // Method
      EqualsInt(int64) (bool, error)
      // Method
      EqualsString(string) (bool, error)
      // Method
      Err() error
      // Method
      Float64() (float64, error)
      // Method
      Floor() maths.Decimal
      // Method
      Format(string) (string, error)
      // Method
      GetPercent(maths.Decimal) maths.Decimal
      // Method
      GetPercentFloat(float64) maths.Decimal
      // Method
      GetPercentInt(int64) maths.Decimal
      // Method
      GetPercentString(string) maths.Decimal
      // Method
      GreaterThan(maths.Decimal) (bool, error)
      // Method
      GreaterThanFloat(float64) (bool, error)
      // Method
      GreaterThanInt(int64) (bool, error)
      // Method
      GreaterThanOrEqual(maths.Decimal) (bool, error)
      // Method
      GreaterThanString(string) (bool, error)
      // Method
      Int64() (int64, error)
      // Method
      IsBetween(maths.Decimal, maths.Decimal) (bool, error)
      // Method
      IsCloseTo(maths.Decimal, maths.Decimal) (bool, error)
      // Method
      IsEven() (bool, error)
      // Method
      IsInteger() (bool, error)
      // Method
      IsMultipleOf(maths.Decimal) (bool, error)
      // Method
      IsNegative() (bool, error)
      // Method
      IsOdd() (bool, error)
      // Method
      IsPositive() (bool, error)
      // Method
      IsZero() (bool, error)
      // Method
      LessThan(maths.Decimal) (bool, error)
      // Method
      LessThanFloat(float64) (bool, error)
      // Method
      LessThanInt(int64) (bool, error)
      // Method
      LessThanOrEqual(maths.Decimal) (bool, error)
      // Method
      LessThanString(string) (bool, error)
      // Method
      MarshalJSON() ([]uint8, error)
      // Method
      Modulus(maths.Decimal) maths.Decimal
      // Method
      ModulusBigInt(maths.BigInt) maths.Decimal
      // Method
      ModulusDecimal(maths.Decimal) maths.Decimal
      // Method
      ModulusFloat(float64) maths.Decimal
      // Method
      ModulusInt(int64) maths.Decimal
      // Method
      ModulusString(string) maths.Decimal
      // Method
      Multiply(maths.Decimal) maths.Decimal
      // Method
      MultiplyBigInt(maths.BigInt) maths.Decimal
      // Method
      MultiplyDecimal(maths.Decimal) maths.Decimal
      // Method
      MultiplyFloat(float64) maths.Decimal
      // Method
      MultiplyInPlace(maths.Decimal)
      // Method
      MultiplyInt(int64) maths.Decimal
      // Method
      MultiplyString(string) maths.Decimal
      // Method
      Must() maths.Decimal
      // Method
      MustEquals(maths.Decimal) bool
      // Method
      MustFloat64() float64
      // Method
      MustFormat(string) string
      // Method
      MustGreaterThan(maths.Decimal) bool
      // Method
      MustInt64() int64
      // Method
      MustIsBetween(maths.Decimal, maths.Decimal) bool
      // Method
      MustIsCloseTo(maths.Decimal, maths.Decimal) bool
      // Method
      MustIsEven() bool
      // Method
      MustIsInteger() bool
      // Method
      MustIsMultipleOf(maths.Decimal) bool
      // Method
      MustIsNegative() bool
      // Method
      MustIsOdd() bool
      // Method
      MustIsPositive() bool
      // Method
      MustIsZero() bool
      // Method
      MustLessThan(maths.Decimal) bool
      // Method
      MustString() string
      // Method
      Negate() maths.Decimal
      // Method
      Power(maths.Decimal) maths.Decimal
      // Method
      PowerBigInt(maths.BigInt) maths.Decimal
      // Method
      PowerDecimal(maths.Decimal) maths.Decimal
      // Method
      PowerFloat(float64) maths.Decimal
      // Method
      PowerInt(int64) maths.Decimal
      // Method
      PowerString(string) maths.Decimal
      // Method
      Remainder(maths.Decimal) maths.Decimal
      // Method
      RemainderBigInt(maths.BigInt) maths.Decimal
      // Method
      RemainderDecimal(maths.Decimal) maths.Decimal
      // Method
      RemainderFloat(float64) maths.Decimal
      // Method
      RemainderInt(int64) maths.Decimal
      // Method
      RemainderString(string) maths.Decimal
      // Method
      Round(int32) maths.Decimal
      // Method
      Scan(interface {}) error
      // Method
      String() (string, error)
      // Method
      Subtract(maths.Decimal) maths.Decimal
      // Method
      SubtractBigInt(maths.BigInt) maths.Decimal
      // Method
      SubtractDecimal(maths.Decimal) maths.Decimal
      // Method
      SubtractFloat(float64) maths.Decimal
      // Method
      SubtractInPlace(maths.Decimal)
      // Method
      SubtractInt(int64) maths.Decimal
      // Method
      SubtractPercent(maths.Decimal) maths.Decimal
      // Method
      SubtractPercentFloat(float64) maths.Decimal
      // Method
      SubtractPercentInt(int64) maths.Decimal
      // Method
      SubtractPercentString(string) maths.Decimal
      // Method
      SubtractString(string) maths.Decimal
      // Method
      ToBigInt() maths.BigInt
      // Method
      Truncate() maths.Decimal
      // Method
      UnmarshalJSON([]uint8) error
      // Method
      Value() (driver.Value, error)
      // Method
      When(bool, func(maths.Decimal) maths.Decimal) maths.Decimal
      // Method
      WhenBetween(maths.Decimal, maths.Decimal, func(maths.Decimal) maths.Decimal) maths.Decimal
      // Method
      WhenCloseTo(maths.Decimal, maths.Decimal, func(maths.Decimal) maths.Decimal) maths.Decimal
      // Method
      WhenEven(func(maths.Decimal) maths.Decimal) maths.Decimal
      // Method
      WhenInteger(func(maths.Decimal) maths.Decimal) maths.Decimal
      // Method
      WhenMultipleOf(maths.Decimal, func(maths.Decimal) maths.Decimal) maths.Decimal
      // Method
      WhenNegative(func(maths.Decimal) maths.Decimal) maths.Decimal
      // Method
      WhenOdd(func(maths.Decimal) maths.Decimal) maths.Decimal
      // Method
      WhenPositive(func(maths.Decimal) maths.Decimal) maths.Decimal
      // Method
      WhenZero(func(maths.Decimal) maths.Decimal) maths.Decimal
  }
ExchangeRates: |- # Kind: Struct
  struct {
      // Field
      Rates map[string]maths.Decimal
      // Field
      BaseCurrency string
  }
HundredBigInt: func() maths.BigInt # Kind: Func
HundredDecimal: func() maths.Decimal # Kind: Func
HundredMoney: func(string) maths.Money # Kind: Func
InvertRates: func(maths.ExchangeRates, string) (maths.ExchangeRates, error) # Kind: Func
MatrixConverter: |- # Kind: Struct
  struct {
      // (+1 unexported fields)

      // Method
      CanConvert(string, string) bool
      // Method
      Convert(maths.Money, string) maths.Money
      // Method
      ConvertAll([]maths.Money, string) ([]maths.Money, error)
      // Method
      Supports(string) bool
  }
MaxBigInt: func(maths.BigInt, ...maths.BigInt) maths.BigInt # Kind: Func
MaxDecimal: func(maths.Decimal, ...maths.Decimal) maths.Decimal # Kind: Func
MaxMoney: func(maths.Money, ...maths.Money) maths.Money # Kind: Func
MinBigInt: func(maths.BigInt, ...maths.BigInt) maths.BigInt # Kind: Func
MinDecimal: func(maths.Decimal, ...maths.Decimal) maths.Decimal # Kind: Func
MinMoney: func(maths.Money, ...maths.Money) maths.Money # Kind: Func
Money: |- # Kind: Struct
  struct {
      // (+2 unexported fields)

      // Method
      Abs() maths.Money
      // Method
      Add(maths.Money) maths.Money
      // Method
      AddBigInt(maths.BigInt) maths.Money
      // Method
      AddDecimal(maths.Decimal) maths.Money
      // Method
      AddFloat(float64) maths.Money
      // Method
      AddInPlace(maths.Money)
      // Method
      AddInt(int64) maths.Money
      // Method
      AddMinorInt(int64) maths.Money
      // Method
      AddPercent(maths.Decimal) maths.Money
      // Method
      AddPercentFloat(float64) maths.Money
      // Method
      AddPercentInt(int64) maths.Money
      // Method
      AddPercentString(string) maths.Money
      // Method
      AddString(string) maths.Money
      // Method
      Allocate(...int64) ([]maths.Money, error)
      // Method
      Amount() (maths.Decimal, error)
      // Method
      AsPercentOf(maths.Money) maths.Decimal
      // Method
      AsPercentOfFloat(float64) maths.Decimal
      // Method
      AsPercentOfInt(int64) maths.Decimal
      // Method
      AsPercentOfString(string) maths.Decimal
      // Method
      Ceil() maths.Money
      // Method
      CheckEquals(maths.Money) bool
      // Method
      CheckGreaterThan(maths.Money) bool
      // Method
      CheckIsBetween(maths.Money, maths.Money) bool
      // Method
      CheckIsCloseTo(maths.Money, maths.Money) bool
      // Method
      CheckIsEven() bool
      // Method
      CheckIsInteger() bool
      // Method
      CheckIsMultipleOf(maths.Money) bool
      // Method
      CheckIsNegative() bool
      // Method
      CheckIsOdd() bool
      // Method
      CheckIsPositive() bool
      // Method
      CheckIsZero() bool
      // Method
      CheckLessThan(maths.Money) bool
      // Method
      CurrencyCode() (string, error)
      // Method
      DefaultFormat() string
      // Method
      Divide(maths.Decimal) maths.Money
      // Method
      DivideBigInt(maths.BigInt) maths.Money
      // Method
      DivideFloat(float64) maths.Money
      // Method
      DivideInt(int64) maths.Money
      // Method
      DivideString(string) maths.Money
      // Method
      Equals(maths.Money) (bool, error)
      // Method
      EqualsFloat(float64) (bool, error)
      // Method
      EqualsInt(int64) (bool, error)
      // Method
      EqualsString(string) (bool, error)
      // Method
      Err() error
      // Method
      Floor() maths.Money
      // Method
      Format(string) (string, error)
      // Method
      FormattedNumber() (string, error)
      // Method
      GetPercent(maths.Decimal) maths.Money
      // Method
      GetPercentFloat(float64) maths.Money
      // Method
      GetPercentInt(int64) maths.Money
      // Method
      GetPercentString(string) maths.Money
      // Method
      GreaterThan(maths.Money) (bool, error)
      // Method
      GreaterThanFloat(float64) (bool, error)
      // Method
      GreaterThanInt(int64) (bool, error)
      // Method
      GreaterThanString(string) (bool, error)
      // Method
      IsBetween(maths.Money, maths.Money) (bool, error)
      // Method
      IsCloseTo(maths.Money, maths.Money) (bool, error)
      // Method
      IsEven() (bool, error)
      // Method
      IsInteger() (bool, error)
      // Method
      IsMultipleOf(maths.Money) (bool, error)
      // Method
      IsNegative() (bool, error)
      // Method
      IsOdd() (bool, error)
      // Method
      IsPositive() (bool, error)
      // Method
      IsZero() (bool, error)
      // Method
      LessThan(maths.Money) (bool, error)
      // Method
      LessThanFloat(float64) (bool, error)
      // Method
      LessThanInt(int64) (bool, error)
      // Method
      LessThanString(string) (bool, error)
      // Method
      MarshalJSON() ([]uint8, error)
      // Method
      Modulus(maths.Decimal) maths.Money
      // Method
      ModulusBigInt(maths.BigInt) maths.Money
      // Method
      ModulusFloat(float64) maths.Money
      // Method
      ModulusInt(int64) maths.Money
      // Method
      ModulusString(string) maths.Money
      // Method
      Multiply(maths.Decimal) maths.Money
      // Method
      MultiplyBigInt(maths.BigInt) maths.Money
      // Method
      MultiplyFloat(float64) maths.Money
      // Method
      MultiplyInPlace(maths.Decimal)
      // Method
      MultiplyInt(int64) maths.Money
      // Method
      MultiplyString(string) maths.Money
      // Method
      Must() maths.Money
      // Method
      MustEquals(maths.Money) bool
      // Method
      MustFormat(string) string
      // Method
      MustFormattedNumber() string
      // Method
      MustGreaterThan(maths.Money) bool
      // Method
      MustIsBetween(maths.Money, maths.Money) bool
      // Method
      MustIsCloseTo(maths.Money, maths.Money) bool
      // Method
      MustIsEven() bool
      // Method
      MustIsInteger() bool
      // Method
      MustIsMultipleOf(maths.Money) bool
      // Method
      MustIsNegative() bool
      // Method
      MustIsOdd() bool
      // Method
      MustIsPositive() bool
      // Method
      MustIsZero() bool
      // Method
      MustLessThan(maths.Money) bool
      // Method
      MustNumber() string
      // Method
      MustRoundedNumber() string
      // Method
      MustString() string
      // Method
      Negate() maths.Money
      // Method
      Number() (string, error)
      // Method
      Remainder(maths.Decimal) maths.Money
      // Method
      RemainderBigInt(maths.BigInt) maths.Money
      // Method
      RemainderFloat(float64) maths.Money
      // Method
      RemainderInt(int64) maths.Money
      // Method
      RemainderString(string) maths.Money
      // Method
      RoundTo(uint8, currency.RoundingMode) maths.Money
      // Method
      RoundToStandard() maths.Money
      // Method
      RoundedDefaultFormat() string
      // Method
      RoundedNumber() (string, error)
      // Method
      Scan(interface {}) error
      // Method
      String() (string, error)
      // Method
      Subtract(maths.Money) maths.Money
      // Method
      SubtractBigInt(maths.BigInt) maths.Money
      // Method
      SubtractDecimal(maths.Decimal) maths.Money
      // Method
      SubtractFloat(float64) maths.Money
      // Method
      SubtractInPlace(maths.Money)
      // Method
      SubtractInt(int64) maths.Money
      // Method
      SubtractMinorInt(int64) maths.Money
      // Method
      SubtractPercent(maths.Decimal) maths.Money
      // Method
      SubtractPercentFloat(float64) maths.Money
      // Method
      SubtractPercentInt(int64) maths.Money
      // Method
      SubtractPercentString(string) maths.Money
      // Method
      SubtractString(string) maths.Money
      // Method
      Truncate() maths.Money
      // Method
      UnmarshalJSON([]uint8) error
      // Method
      Value() (driver.Value, error)
      // Method
      When(bool, func(maths.Money) maths.Money) maths.Money
      // Method
      WhenBetween(maths.Money, maths.Money, func(maths.Money) maths.Money) maths.Money
      // Method
      WhenCloseTo(maths.Money, maths.Money, func(maths.Money) maths.Money) maths.Money
      // Method
      WhenEven(func(maths.Money) maths.Money) maths.Money
      // Method
      WhenInteger(func(maths.Money) maths.Money) maths.Money
      // Method
      WhenMultipleOf(maths.Money, func(maths.Money) maths.Money) maths.Money
      // Method
      WhenNegative(func(maths.Money) maths.Money) maths.Money
      // Method
      WhenOdd(func(maths.Money) maths.Money) maths.Money
      // Method
      WhenPositive(func(maths.Money) maths.Money) maths.Money
      // Method
      WhenZero(func(maths.Money) maths.Money) maths.Money
  }
NewBigIntFromApd: func(apd.BigInt) maths.BigInt # Kind: Func
NewBigIntFromInt: func(int64) maths.BigInt # Kind: Func
NewBigIntFromString: func(string) maths.BigInt # Kind: Func
NewConverter: func(maths.ExchangeRates) *maths.Converter # Kind: Func
NewDecimalFromApd: func(apd.Decimal) maths.Decimal # Kind: Func
NewDecimalFromFloat: func(float64) maths.Decimal # Kind: Func
NewDecimalFromInt: func(int64) maths.Decimal # Kind: Func
NewDecimalFromString: func(string) maths.Decimal # Kind: Func
NewExchangeRates: func(string, map[string]maths.Decimal) (maths.ExchangeRates, error) # Kind: Func
NewMatrixConverter: func(maths.RateMatrix) *maths.MatrixConverter # Kind: Func
NewMoneyFromDecimal: func(maths.Decimal, string) maths.Money # Kind: Func
NewMoneyFromFloat: func(float64, string) maths.Money # Kind: Func
NewMoneyFromInt: func(int64, string) maths.Money # Kind: Func
NewMoneyFromMinorInt: func(int64, string) maths.Money # Kind: Func
NewMoneyFromString: func(string, string) maths.Money # Kind: Func
NewRateMatrix: func(string, map[string]maths.Decimal, map[string]map[string]maths.Decimal) (maths.RateMatrix, error) # Kind: Func
OneBigInt: func() maths.BigInt # Kind: Func
OneDecimal: func() maths.Decimal # Kind: Func
OneMoney: func(string) maths.Money # Kind: Func
RateMatrix: |- # Kind: Struct
  struct {
      // Field
      Rates map[string]map[string]maths.Decimal
      // Field
      BaseCurrency string
  }
RegisterCurrency: func(string, currency.Definition) # Kind: Func
SortBigInts: func([]maths.BigInt) error # Kind: Func
SortBigIntsReverse: func([]maths.BigInt) error # Kind: Func
SortDecimals: func([]maths.Decimal) error # Kind: Func
SortDecimalsReverse: func([]maths.Decimal) error # Kind: Func
SortMoney: func([]maths.Money) error # Kind: Func
SortMoneyReverse: func([]maths.Money) error # Kind: Func
SumBigInts: func(...maths.BigInt) maths.BigInt # Kind: Func
SumDecimals: func(...maths.Decimal) maths.Decimal # Kind: Func
SumMoney: func(...maths.Money) maths.Money # Kind: Func
TenBigInt: func() maths.BigInt # Kind: Func
TenDecimal: func() maths.Decimal # Kind: Func
ZeroBigInt: func() maths.BigInt # Kind: Func
ZeroBigIntWithError: func(error) maths.BigInt # Kind: Func
ZeroDecimal: func() maths.Decimal # Kind: Func
ZeroDecimalWithError: func(error) maths.Decimal # Kind: Func
ZeroMoney: func(string) maths.Money # Kind: Func
ZeroMoneyWithError: func(string, error) maths.Money # Kind: Func
