(() => {
    const el = document.querySelector({{jsStr .Selector}});
    if (!el) return null;
    const rect = el.getBoundingClientRect();
    return {
        x: rect.x,
        y: rect.y,
        width: rect.width,
        height: rect.height
    };
})()
