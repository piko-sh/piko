{
  "description": "INSERT ... ON CONFLICT (col) WHERE <predicate> DO NOTHING / DO UPDATE ... RETURNING. The conflict-target WHERE predicate must be consumed so the trailing RETURNING columns and the DO UPDATE SET parameter are not silently dropped."
}
