{
  "description": "Query-level piko.column with `type:` overrides the analyser's inferred SQL type on a single output column. The LOWER(email) expression has no source-column lineage, so the override is the only way to set its type."
}
