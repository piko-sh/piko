{
  "description": "piko.column references a non-existent output column on MySQL; analyser emits Q036.",
  "diagnostics": [
    { "severity": "error", "code": "Q036", "messageContains": "unknown output column" }
  ]
}
