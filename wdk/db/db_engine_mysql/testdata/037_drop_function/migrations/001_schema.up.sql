CREATE TABLE values_table (
    id INT AUTO_INCREMENT PRIMARY KEY,
    amount INT NOT NULL
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE FUNCTION triple_amount(val INT)
RETURNS INT DETERMINISTIC NO SQL
RETURN val * 3;
