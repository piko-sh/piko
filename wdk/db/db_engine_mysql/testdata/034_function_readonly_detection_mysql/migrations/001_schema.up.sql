CREATE TABLE scores (
    id INT AUTO_INCREMENT PRIMARY KEY,
    player VARCHAR(100) NOT NULL,
    points INT NOT NULL DEFAULT 0
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE FUNCTION safe_multiply(a INT, b INT)
RETURNS INT DETERMINISTIC NO SQL
RETURN a * b;
