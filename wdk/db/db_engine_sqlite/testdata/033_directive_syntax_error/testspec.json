{
  "description": "Query has an invalid command keyword-argument value, producing a single Q028 invalid-keyword-argument-value diagnostic with a 'did you mean' suggestion from the closed enum. The downstream applyTopCall command-parse failure is silenced because the validator already reported the problem.",
  "expectedDiagnosticCount": 1,
  "diagnostics": [
    { "severity": "error", "code": "Q028", "messageContains": "invalid value" }
  ]
}
