{
  "description": "JSON access operators -> (returns JSON) and ->> (extracts text). Verifies tokenisation, parsing as BinaryOpExpression, and type resolution (-> returns nullable json, ->> returns nullable text)."
}
