{
  "description": "Two queries with missing required directives: one missing piko.name, the other missing piko.command. Both produce Q008 errors.",
  "expectedDiagnosticCount": 2,
  "diagnostics": [
    { "severity": "error", "code": "Q008", "messageContains": "piko.name", "filename": "missing_name.sql" },
    { "severity": "error", "code": "Q008", "messageContains": "piko.command", "filename": "missing_command.sql" }
  ]
}
