<!--
  Copyright 2026 PolitePixels Limited

  Licensed under the Apache License, Version 2.0 (the "License");
  you may not use this file except in compliance with the License.
  You may obtain a copy of the License at

      http://www.apache.org/licenses/LICENSE-2.0

  Unless required by applicable law or agreed to in writing, software
  distributed under the License is distributed on an "AS IS" BASIS,
  WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
  See the License for the specific language governing permissions and
  limitations under the License.

  This project stands against fascism, authoritarianism, and all forms of
  oppression. We built this to empower people, not to enable those who would
  strip others of their rights and dignity.
-->

<template name="piko-counter">
    <div class="counter">
        <p class="value" p-class="{ negative: state.count < 0 }" p-text="state.count"></p>
        <div class="buttons">
            <button p-on:click="decrement">-</button>
            <button p-on:click="increment">+</button>
        </div>
    </div>
</template>

<script lang="ts">
    const state = {
        count: 0 as number,
    };

    function increment() {
        state.count++;
    }

    function decrement() {
        state.count--;
    }
</script>

<style>
    :host {
        display: inline-block;

        --_accent: var(--piko-counter-accent, var(--g-colour-secondary-700, #39ABA1));
        --_accent-hover: var(--piko-counter-accent-hover, var(--g-colour-secondary-800, #2E8B83));
        --_negative: var(--piko-counter-negative, #dc2626);
        --_surface: var(--piko-counter-surface, #ffffff);
        --_border: var(--piko-counter-border, var(--g-colour-grey-200, #CAD1D8));
        --_radius: var(--piko-counter-radius, var(--g-radius-lg, 20px));
        --_button-radius: var(--piko-counter-button-radius, var(--g-radius-max, 9999px));

        font-family: var(--g-font-family, sans-serif);
    }

    .counter {
        display: inline-flex;
        align-items: center;
        gap: 0.75rem;
        padding: 0.5rem 0.75rem;
        border: 1px solid var(--_border);
        border-radius: var(--_radius);
        background: var(--_surface);
        font-family: var(--g-font-family, sans-serif);
    }

    .value {
        min-width: 2.5rem;
        margin: 0;
        font-size: 1.5rem;
        font-weight: 700;
        text-align: center;
        color: var(--_accent);
        transition: color 0.2s ease;
    }

    .value.negative {
        color: var(--_negative);
    }

    .buttons {
        display: flex;
        gap: 0.375rem;
    }

    button {
        font-family: inherit;
        width: 2rem;
        height: 2rem;
        font-size: 1.125rem;
        font-weight: 600;
        border: none;
        border-radius: var(--_button-radius);
        background-color: var(--_accent);
        color: #ffffff;
        cursor: pointer;
        transition: background-color 0.15s ease;
    }

    button:hover,
    button:active {
        background-color: var(--_accent-hover);
    }
</style>
