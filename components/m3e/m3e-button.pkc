<!--
  Copyright 2026 PolitePixels Limited

  Licensed under the Apache License, Version 2.0 (the "License");
  you may not use this file except in compliance with the License.
  You may obtain a copy of the License at

      http://www.apache.org/licenses/LICENSE-2.0

  Unless required by applicable law or agreed to in writing, software
  distributed under the License is distributed on an "AS IS" BASIS,
  WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
  See the License for the specific language governing permissions and
  limitations under the License.

  This project stands against fascism, authoritarianism, and all forms of
  oppression. We built this to empower people, not to enable those who would
  strip others of their rights and dignity.
-->

<template name="m3e-button">
    <a p-if="state.href" class="button"
       p-class="{
           'elevated': state.variant == 'elevated',
           'filled': state.variant == 'filled',
           'filled-tonal': state.variant == 'filled-tonal',
           'outlined': state.variant == 'outlined',
           'text': state.variant == 'text',
           'disabled': state.disabled,
           'soft-disabled': state.soft_disabled,
           'size-xs': state.size == 'xs',
           'size-s': state.size == 's',
           'size-m': state.size == 'medium' || state.size == 'm',
           'size-l': state.size == 'l',
           'size-xl': state.size == 'xl',
           'shape-square': state.shape == 'square',
           'toggle': state.toggle,
           'selected': state.selected
       }"
       :href="state.disabled || state.soft_disabled ? null : state.href"
       :tabindex="state.disabled ? '-1' : '0'"
       :aria-disabled="state.disabled || state.soft_disabled ? 'true' : 'false'"
       :aria-pressed="state.toggle ? (state.selected ? 'true' : 'false') : null"
       p-on:click="handleClick">
        <span class="state-layer"></span>
        <span class="icon-slot" p-show="state.has_icon"><slot name="icon"></slot></span>
        <span class="label"><slot></slot></span>
    </a>
    <button p-if="!state.href" class="button"
            p-class="{
                'elevated': state.variant == 'elevated',
                'filled': state.variant == 'filled',
                'filled-tonal': state.variant == 'filled-tonal',
                'outlined': state.variant == 'outlined',
                'text': state.variant == 'text',
                'disabled': state.disabled,
                'soft-disabled': state.soft_disabled,
                'size-xs': state.size == 'xs',
                'size-s': state.size == 's',
                'size-m': state.size == 'medium' || state.size == 'm',
                'size-l': state.size == 'l',
                'size-xl': state.size == 'xl',
                'shape-square': state.shape == 'square',
                'toggle': state.toggle,
                'selected': state.selected
            }"
            :disabled="state.disabled ? '' : null"
            :aria-disabled="state.disabled || state.soft_disabled ? 'true' : 'false'"
            :aria-pressed="state.toggle ? (state.selected ? 'true' : 'false') : null"
            type="button"
            p-on:click="handleClick">
        <span class="state-layer"></span>
        <span class="icon-slot" p-show="state.has_icon"><slot name="icon"></slot></span>
        <span class="label"><slot></slot></span>
    </button>
</template>

<script lang="ts">
    const state = {
        variant: 'filled' as string,
        size: 'medium' as string,
        shape: 'round' as string,
        disabled: false as boolean,
        soft_disabled: false as boolean,
        toggle: false as boolean,
        selected: false as boolean,
        href: '' as string,
        has_icon: false as boolean,
    };

    pkc.onConnected(() => {
        console.log("onConnected")
    });

    pkc.attachSlotListener('icon', (elements) => {
        state.has_icon = elements.length > 0;
    });

    function handleClick() {
        if (state.disabled || state.soft_disabled) return;

        if (state.toggle) {
            state.selected = !state.selected;
            pkc.dispatchEvent(new CustomEvent('change', {
                detail: { selected: state.selected },
                bubbles: true,
                composed: true,
            }));
        }
    }
</script>

<style>
    :host {
        display: inline-flex;
        flex-shrink: 0;
        --_primary: var(--m3e-primary, #6750a4);
        --_on-primary: var(--m3e-on-primary, #ffffff);
        --_primary-container: var(--m3e-primary-container, #eaddff);
        --_on-primary-container: var(--m3e-on-primary-container, #21005d);
        --_secondary-container: var(--m3e-secondary-container, #e8def8);
        --_on-secondary-container: var(--m3e-on-secondary-container, #1d192b);
        --_surface: var(--m3e-surface, #fffbfe);
        --_on-surface: var(--m3e-on-surface, #1c1b1f);
        --_on-surface-variant: var(--m3e-on-surface-variant, #49454f);
        --_outline: var(--m3e-outline, #79747e);
        --_surface-container-low: var(--m3e-surface-container-low, #f7f2fa);
        --_duration-short: var(--m3e-duration-short, 200ms);
        --_easing-standard: var(--m3e-easing-standard, cubic-bezier(0.2, 0, 0, 1));
        --_shape-full: var(--m3e-shape-full, 9999px);
        font-family: system-ui, -apple-system, sans-serif;

        &([touch-target="wrapper"]) {
            margin: 4px 0;
        }
    }

    .button {
        position: relative;
        display: inline-flex;
        align-items: center;
        justify-content: center;
        gap: 8px;
        height: 40px;
        padding: 0 24px;
        border: none;
        border-radius: var(--_shape-full);
        font-size: 0.875rem;
        font-weight: 500;
        letter-spacing: 0.1px;
        text-decoration: none;
        cursor: pointer;
        outline: none;
        overflow: hidden;
        -webkit-tap-highlight-color: transparent;
        transition: box-shadow var(--_duration-short) var(--_easing-standard);

        &:focus-visible .state-layer {
            opacity: 0.12;
        }

        &:hover .state-layer {
            opacity: 0.08;
        }

        &:active .state-layer {
            opacity: 0.12;
        }
    }

    .icon-slot {
        display: inline-flex;
        align-items: center;
    }

    .label {
        display: inline-flex;
        align-items: center;
    }

    .state-layer {
        position: absolute;
        inset: 0;
        border-radius: inherit;
        opacity: 0;
        transition: opacity var(--_duration-short);
        pointer-events: none;
    }

    .elevated {
        background: var(--_surface-container-low);
        color: var(--_primary);
        box-shadow: 0px 1px 2px 0px rgba(0,0,0,0.30), 0px 1px 3px 1px rgba(0,0,0,0.15);

        & .state-layer {
            background: var(--_primary);
        }

        &:hover {
            box-shadow: 0px 1px 2px 0px rgba(0,0,0,0.30), 0px 2px 6px 2px rgba(0,0,0,0.15);
        }
    }

    .filled {
        background: var(--_primary);
        color: var(--_on-primary);

        & .state-layer {
            background: var(--_on-primary);
        }

        &:hover {
            box-shadow: 0px 1px 2px 0px rgba(0,0,0,0.30), 0px 1px 3px 1px rgba(0,0,0,0.15);
        }
    }

    .filled-tonal {
        background: var(--_secondary-container);
        color: var(--_on-secondary-container);

        & .state-layer {
            background: var(--_on-secondary-container);
        }

        &:hover {
            box-shadow: 0px 1px 2px 0px rgba(0,0,0,0.30), 0px 1px 3px 1px rgba(0,0,0,0.15);
        }
    }

    .outlined {
        background: transparent;
        color: var(--_primary);
        border: 1px solid var(--_outline);

        & .state-layer {
            background: var(--_primary);
        }
    }

    .text {
        background: transparent;
        color: var(--_primary);
        padding: 0 12px;

        & .state-layer {
            background: var(--_primary);
        }
    }

    .disabled {
        cursor: default;
        pointer-events: none;
        opacity: 0.38;
        box-shadow: none;

        &.outlined {
            border-color: var(--_on-surface);
        }
    }

    .soft-disabled {
        opacity: 0.38;
    }

    .size-xs {
        height: 28px;
        padding: 0 12px;
        font-size: 0.75rem;
        gap: 4px;

        &.text {
            padding: 0 8px;
        }
    }

    .size-s {
        height: 34px;
        padding: 0 18px;
        font-size: 0.8125rem;
        gap: 6px;

        &.text {
            padding: 0 8px;
        }
    }

    .size-l {
        height: 48px;
        padding: 0 32px;
        font-size: 1rem;
        gap: 10px;

        &.text {
            padding: 0 16px;
        }
    }

    .size-xl {
        height: 56px;
        padding: 0 40px;
        font-size: 1.125rem;
        gap: 12px;

        &.text {
            padding: 0 20px;
        }
    }

    .shape-square {
        border-radius: var(--m3e-shape-sm, 8px);
    }

    .toggle.selected {
        &.filled {
            background: var(--_primary-container);
            color: var(--_on-primary-container);

            & .state-layer {
                background: var(--_on-primary-container);
            }
        }

        &.outlined {
            background: var(--_secondary-container);
            color: var(--_on-secondary-container);
            border-color: transparent;
        }

        &.elevated,
        &.text {
            color: var(--_primary);
            font-weight: 600;
        }
    }
</style>
