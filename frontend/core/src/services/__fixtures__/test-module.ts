/** Hashed partial identifier for the test fixture. */
// eslint-disable-next-line @typescript-eslint/naming-convention
export const __PARTIAL_ID__ = 'fixture-partial-hash';
/** Test fixture function. */
export function fixtureFunc() { return 'fixture-result'; }
/** Non-function export used to verify that only functions are resolved. */
export const nonFunctionExport = 'not-a-function';
